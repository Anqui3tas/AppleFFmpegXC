/// Shim target that links the prebuilt FFmpeg XCFrameworks.
/// Import `FFmpeg` to make the C symbols from the bundled `libav*`
/// libraries available to your app and pull in the binary targets.
public enum FFmpeg {}
